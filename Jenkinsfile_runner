pipeline {
    agent any
    stages {
        stage ("Run Test") {
            steps {
                sh "docker-compose up"
            }
        }
        stage ("Teardown environment") {
            steps {
                sh "docker-compose down"
            }
        }
    }
}